# Template of environment variables for Render (copy values into Render Dashboard as secrets or env vars)
# Fill with real production values before deploying.

# MongoDB connection string (required)
# Example: mongodb+srv://user:password@cluster.mongodb.net/mydatabase?retryWrites=true&w=majority
MONGODB_URI="mongodb+srv://<MONGO_USER>:<MONGO_PASSWORD>@<CLUSTER_HOST>/<DATABASE>?retryWrites=true&w=majority"

# Spring Boot server port (optional, default 8080)
PORT=8080

# Mail configuration (required if you send emails)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=seu-email@example.com
MAIL_PASSWORD="SUA_MAIL_APP_PASSWORD_AQUI"
MAIL_SMTP_AUTH=true
MAIL_SMTP_STARTTLS_ENABLE=true

# Sender address used by app (optional - defaults to spring.mail.username)
APP_EMAIL_FROM=no-reply@seu-dominio.com

# Frontend base URL used to build verification/reset links (optional, but recommended)
APP_FRONT_BASE_URL=https://app.seu-dominio.com
APP_FRONT_VERIFY_PATH=/verify-email
APP_FRONT_RESET_PATH=/reset-password

# Redis (if you use caching / cart / checkout)
# Use a managed Redis URL or create one on Render; include password if required: redis://:password@host:port
REDIS_URL=redis://localhost:6379
SPRING_REDIS_HOST=localhost
SPRING_REDIS_PORT=6379

# JWT configuration (required)
# jwt.secret.base64 must be a base64-encoded secret key string the application will use to sign tokens
JWT_SECRET_BASE64="<BASE64_SECRET>"
JWT_ISSUER=v-disk
JWT_AUDIENCE=users
JWT_TTL_SECONDS=3600

# Optional application timeouts
APP_CHECKOUT_TTL_SECONDS=1800
APP_CART_TTL_SECONDS=86400

# Other flags
# If you want to explicitly disable spring docker compose integration (already set in app.yml), not necessary here
# SPRING_DOCKER_COMPOSE_ENABLED=false

# Notes for Render
# - Mark secrets (passwords, MONGODB_URI, MAIL_PASSWORD, JWT_SECRET_BASE64) as "Secret" in Render Dashboard.
# - If using Render's managed Postgres/Redis, use the service connection string for REDIS_URL.
# - Use proper escaping/quoting for values that include special characters.
