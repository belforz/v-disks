# Exemplo de .env — copie para .env e preencha os valores reais.

# MongoDB (URI com nome da database)
# Exemplo: mongodb+srv://user:password@cluster.mongodb.net/mydatabase?retryWrites=true&w=majority
MONGODB_URI="mongodb+srv://<MONGO_USER>:<MONGO_PASSWORD>@<CLUSTER_HOST>/<DATABASE>?retryWrites=true&w=majority"

# Mail (use App Password se for Gmail)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=seu-email@example.com
MAIL_PASSWORD="SUA_MAIL_APP_PASSWORD_AQUI"
MAIL_SMTP_AUTH=true
MAIL_SMTP_STARTTLS_ENABLE=true

# App settings (opcionais)
APP_EMAIL_FROM=no-reply@seu-dominio.com
APP_EMAIL_VERIFY_URL=https://app.seu-dominio.com/verify-email

# Cache / Redis
CACHE_TYPE=redis

# Redis URL (sem senha por padrão). Se usar senha:
REDIS_URL=redis://localhost:6379